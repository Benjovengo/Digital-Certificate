import React, { useLayoutEffect } from 'react'

const Governance = () => {

  /** Start the page at the top */
  useLayoutEffect(() => {
    window.scrollTo(0, 0)
  });

  
  return (
    <div>Governance</div>
  )
}

export default Governance